# Crypto Beats — multiplayer server environment variables
# Copy this file to `.env` and adjust as needed.

# Port the Colyseus server listens on. Defaults to 2567 if unset.
PORT=2567

# Set to "true" for verbose per-event room logging (joins, scores, etc.).
# Warnings and errors are always logged regardless of this setting.
# DEBUG=true
