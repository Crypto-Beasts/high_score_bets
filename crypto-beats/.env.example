# Crypto Beats — client environment variables
# Copy this file to `.env` and adjust as needed. Vite only exposes vars prefixed with VITE_.

# WebSocket URL of the Colyseus multiplayer server.
# Defaults to ws://localhost:2567 if unset.
VITE_SERVER_URL=ws://localhost:2567
